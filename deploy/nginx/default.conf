# Прокси на контейнер Next.js. Домен задайте в DNS; для HTTPS см. ниже.
server {
    listen 80;
    listen [::]:80;
    server_name custom.slavalarionov.com www.custom.slavalarionov.com;

    client_max_body_size 25m;

    # Basic Auth для админки и admin-API.
    location /admin {
        auth_basic           "Admin area";
        auth_basic_user_file /etc/nginx/htpasswd;

        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location /api/admin/ {
        auth_basic           "Admin area";
        auth_basic_user_file /etc/nginx/htpasswd;

        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# Раскомментируйте после получения сертификатов (certbot на хосте или в отдельном контейнере),
# положите fullchain.pem и privkey.pem в deploy/nginx/ssl/ и пересоберите volume:
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name custom.slavalarionov.com www.custom.slavalarionov.com;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     client_max_body_size 25m;
#
#     location / {
#         proxy_pass http://web:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 120s;
#     }
# }
